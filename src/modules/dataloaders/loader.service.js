const userLoaders = require('../user/user.loader');
const commentLoaders = require('../comment/comment.loader');
const replyLoaders = require('../reply/reply.loader');
const likeLoaders = require('../like/like.loader');
const photoLoaders = require('../photo/photo.loader');

module.exports = {
	userLoaders,
	commentLoaders,
	replyLoaders,
	likeLoaders,
	photoLoaders,
};
